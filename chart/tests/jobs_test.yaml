templates:
  - templates/jobs.yaml
tests:
  - it: jobs manifest should match snapshot
    set:
      config.enabled: true
      config.extraWait: 15
    asserts:
      - matchSnapshot:
          path: spec.template.spec
